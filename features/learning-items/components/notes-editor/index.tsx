/**
 * NotesEditor — public re-export. Swap the line below to change the
 * implementation (e.g., to an AI-driven editor). Consumers always import from
 * `./notes-editor` and depend only on `NotesEditorProps` from `./types`.
 */
export { TiptapEditor as NotesEditor } from "./tiptap-editor";
export type { NotesEditorProps } from "./types";
